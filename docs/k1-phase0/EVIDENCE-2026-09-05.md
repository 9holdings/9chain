# Pha 0 · bằng chứng đo `2026-09-05`

Cụm: 3 node `9chain-a1/node:g1-81` (`9chaingo/1.14.2 commit=9chain-a1-g1-27patch-38723877`), băng tập
`networkID 899999998`, C-Chain chainId `9000000909`, netgen `N=3 SUBNET_PREFIX=172.31.0`, máy dev Windows +
Docker 29.6.2 / Compose 5.3.1. Mọi số dưới đây đọc từ API hoặc `/proc` của node đang chạy, không từ mã.

## Đã đo

| # | Phép đo | Kết quả | Kết luận |
|---|---|---|---|
| 0.5 | Tên metric dựng tập validator (`/ext/metrics`) | `avalanche_platformvm_validator_sets_created` · `_cached` · `_duration_sum` · `_height_diff_sum` · `avalanche_network_tracked_subnets` · `avalanche_network_peers` | cổng K1 đọc được, không phải đoán tên |
| 0.4a | `platform.getValidatorFeeConfig` / `FeeState` lúc 0 validator L1 | `capacity 20000 · target 10000 · minPrice 1 · K 865617` · `excess 0 · price 1` | khớp `genesis_9chain_a1.go:103–108`; băng tập chạy đúng bảng A1 |
| — | `fund` X → P (xuất + nhập) | 106 ms + 105 ms; 1.000 LOVE9 lên P-Chain | quỹ netgen nằm **trên X**; P-Chain rỗng — bài "ví hai chặng" đúng cả trên băng tập |
| — | `CreateSubnetTx` → `CreateChainTx` → `ConvertSubnetToL1Tx`, tuần tự một ví | **~3,0 s / sổ** (204+604+1003 ms sổ đầu; 1003+1004+1002 ms các sổ sau — mỗi tx đợi một block P-Chain ~1 s) | 1.000 sổ tuần tự ≈ 50 phút; 10 ví song song ≈ 5 phút — đúng ước §4 PLAN-K1 |
| — | Tên chain có gạch nối | `illegal name character` ⇒ **subnet `gHip8KNjvceJ5DQ2hr6x2h8odEZPnspizbh469GR8YUcugVsa` mồ côi** | mồ côi thật đầu tiên, đúng ca 0.3; công cụ báo đúng; sửa: tên chỉ chữ/số/khoảng trắng |
| 0.2 | L1 **một** validator, **snow mặc định** (không subnet config) | `eth_chainId` đúng; bơm 1 tx/s × 40 s ⇒ **20 block** (0x0 → 0x14), block 1 có 1 tx | **không cần** `subnet-config-dir` k = 1; giữ tệp làm phương án dự phòng |
| 0.4b | Ngủ đông: 2 sổ `Balance 120 nLOVE9` | sau ~2 phút: `balance 0`, `active NO`; bơm 15 tx ⇒ block **đứng ở 0x0**; `getCurrentValidators` **vẫn liệt kê** validator, weight 100, balance 0 | đúng `l1_validator.go:164–194` + `state.go:2686–2714`: mất khả năng đẻ block, **trọng số ở lại** |
| 0.1a | RSS plugin subnet-evm, 1 sổ/node, **im**, knob K1 (16/16/8 MB) | plugin **50–53 MiB**, avalanchego 119 MiB, cgroup ~78 MiB | khớp D-178 (53 MiB lúc im); knob chưa lộ tác dụng khi im — phải đo **dưới tải** |
| 0.6a | `du` DB P-Chain node1 | 36 KiB (0 chain) → **64 KiB** (3 chain, genesis 1.547 B) ⇒ ~9 KiB / sổ (3 tx) | nền cho phép so với khuôn 505 KB |
| gate | `too many tracked subnets` / `StartClose` | **0** trên 3 node, peers = 2 | — |

## Đã đo (tiếp)

| # | Phép đo | Kết quả | Kết luận |
|---|---|---|---|
| — | 27 sổ tuần tự, một ví | **81 s** (`real 1m20.9s`) = 3,0 s/sổ | 1.000 sổ: ~50 phút tuần tự, ~5 phút với 10 ví |
| 0.3 | `apply` chạy lại sau khi xong | `0 new, 30 total` | resume theo `chains.jsonl` đúng |
| 0.3 | Subnet mồ côi | `platform.getSubnets` = 43 = 1 primary + 40 converted + **2 mồ côi** (gHip8K… tên gạch nối; qdWUEJ… genesis 505 KB) | mồ côi đếm được từ chain, khớp hai lần công cụ báo |
| 0.4c | `topup` sổ 1 từ **ví quỹ** (không phải chủ) | tx nhận 103 ms; balance 0 → 10⁷; **15 tx treo được đóng thành 8 block ngay khi thức**, rồi bơm 20 tx ⇒ 0x12 | `IncreaseL1ValidatorBalanceTx` không auth — tài trợ phí là native |
| 0.4d | Sổ ngủ (so-0002) **rồi restart node** | RPC **503** *"chain is not done bootstrapping"*; bootstrapper khởi động ở height 0 và **không xong**; `topup` sau đó **vẫn không** bootstrap sau 120 s | 🔴 **sổ một validator ngủ + node restart = kẹt** cho tới khi có lượt restart nữa (đo tiếp bên dưới). Luật vận hành K1: **nạp phí trước khi restart**, hoặc chấp nhận restart lần hai |
| 0.7 | Restart node với 10 sổ | **10/10 chain bootstrap sau 11 s** (P-Chain 15 s) | bước E của `l1-batch` với 14 sổ/node là phút, không phải giờ |
| 0.1a' | RSS im, **10 sổ/node**, knob K1 | plugin **47–48 MiB** mỗi cái (sum 464–474), avalanchego 127–131, cgroup 212–221 MiB | tuyến tính theo số plugin lúc im |
| 0.6b | Genesis **505 KB** | `CreateChainTx` **bị từ chối**: `packer has insufficient length` — `txs.Codec = codec.NewDefaultManager()` = **256 KiB** (`codec/manager.go:19`) | 🔴 trần thật của genesis là **~256 KiB/tx**, không phải `MaxGenesisLen 1 MiB` (`create_chain_tx.go:20`). Sửa vào `DEEP-DIVE` §6 |
| 0.6c | Genesis **199 KB** × 5 sổ | txDB node1: 1.904 → **5.068 KiB** ⇒ **~633 KiB/sổ** (≈ 3,2× genesis) so với **~18 KiB/sổ** ở genesis 1,5 KB | luật "genesis nhỏ" bằng số: ×35 mỗi sổ, trên **mọi** node |
| — | Phí với ~40 validator thức | `excess 0 · price 1` | ở 0,4 % mục tiêu, không nhích |
| — | `avalanche_network_tracked_subnets` node1 | **36** khi node track 10 | metric đếm subnet node **biết** (tập validator trên P-Chain), không phải `--track-subnets`; gate K1 dùng **log** `too many tracked subnets`, không dùng metric này |

## Đã đo (tiếp 2) — tải và đối chứng

| # | Phép đo | Kết quả | Kết luận |
|---|---|---|---|
| 0.1b | **Tải 3 tx/s × 30 sổ (10/node) × 300 s, knob K1** (16/16/8 MB) | **26.100 tx gửi, 1 lỗi**; block mỗi ~2 s (0x97 ≈ 151 block/300 s); RSS plugin **57 MiB** ở t+150, **60 MiB** ở t+290 (sum ~595); avalanchego 157→191 MiB; cgroup **338 → 399 MiB**/node; CPU **~20 %** một nhân/node | 10 sổ có tải ≈ **0,4 GB · 0,2 nhân** mỗi node ⇒ 15 sổ ≈ 0,6 GB · 0,3 nhân — **thấp hơn** mô hình PLAN-108 (2,7 GB · 1,8 luồng) nhiều lần; `c_tx` thật ≈ 0,007 nhân/(tx/s) kể cả plugin im |
| 0.4d' | Sổ ngủ đã `topup` → **restart node lần hai** | bootstrap **ngay (0 s)**; height 0x4: 15 tx treo lúc ngủ được đóng | đường thức đầy đủ của sổ đã ngủ **khi node từng restart** = nạp phí **+ restart node**. Nạp phí mà không restart thì kẹt (0.4d) |
| gate | sau restart lần hai, 30 sổ, knob mặc định | `status` **30/30**, `StartClose 0`, peers 2 | — |
| 0.1c | RSS **im**, knob **mặc định** | plugin 48–49 MiB — **bằng** knob K1 | cache chỉ lớn **dưới tải**; so sánh có ý nghĩa là lượt tải bên dưới |

| 0.1d | **Tải 3 tx/s × 30 sổ × 300 s, knob MẶC ĐỊNH** (512/512/256 MB) | **27.000 tx, 0 lỗi**; plugin **59 → 62 MiB**; cgroup **345 → 386 MiB**/node; CPU 24 % → 9 % | 🔴 **Đính chính dự đoán của PLAN-K1 §0:** ở tải này cache mặc định **không** làm RAM phình — 512 MB là **trần** của cache trie, chỉ đầy khi trạng thái chain lớn; sổ với vài địa chỉ không đầy. Knob 16/16/8 giữ làm **nắp an toàn**, không phải điều kiện sống chết. Phải đo lại ở K1 thật (7 ngày, trạng thái lớn dần) trước khi kết luận cuối |

| đĩa | **Nền mỗi chain** (`chainData/<blockchainID>/db/pebbledb`) | `du` **288.556 KiB** cho MỌI chain bất kể tx; apparent chỉ 836 KiB, 8 tệp | pebble **cấp trước** WAL ~290 MB/chain ⇒ 15 sổ/node = **4,3 GB nền**; sparse trên ext4 nhưng định cỡ phải tính là thật |
| đĩa | **Byte mỗi tx trên node** (bơm 120 s, 3.600 tx/node) | 2.896.836 → 2.899.992 KiB ⇒ **~900 B/tx/node** (gồm WAL, nhiễu compaction) | 15 sổ × 3 tx/s = 45 tx/s ⇒ **3,5 GB/ngày/node**; r = 1 ⇒ 1,2 GB/ngày/node. Đĩa, không phải RAM, quyết định số node mỗi máy |
| fd | file descriptor node1, 10 sổ | 403 fd tổng, `ulimit -n` 1.048.576 | 15 sổ ≈ 600 fd; không phải rủi ro |

## Kết luận pha 0 — đủ để GO/NO-GO ngày 10 của lịch K1

| Câu hỏi §7 PLAN-K1 | Trả lời đo được |
|---|---|
| 0.1 RAM 15 plugin có tải | ~60 MiB/plugin ở 3 tx/s, **cả hai bộ knob**; 15 sổ ≈ 0,9 GB sum RSS, ~0,6 GB cgroup, ~0,3 nhân. Cỡ node 4 vCPU · 8 GB của PLAN-108 **dư nhiều** |
| 0.2 snow cho 1 validator | **mặc định chạy**; `subnet-config-dir` k = 1 không cần |
| 0.3 `l1-batch` gãy/chạy lại | resume đúng; 2 mồ côi thật, đếm được bằng `platform.getSubnets` |
| 0.4 phí & ngủ đông | `price 1 · excess 0` ở 40 validator; ngủ đúng ~2 phút; thức bằng `topup` từ ví khác; 🔴 **ngủ + restart = kẹt, cần restart lần hai sau khi nạp** |
| 0.5 metric | `avalanche_platformvm_validator_sets_*`; gate P2P dùng **log**, không dùng `tracked_subnets` |
| 0.6 genesis vs txDB | 1,5 KB ⇒ +18 KiB/sổ/node; 199 KB ⇒ +633 KiB/sổ/node; **trần codec 256 KiB** |
| 0.7 bootstrap 10 chain/node | **11 s** |

**Đề xuất:** GO cho K1 với các sửa: `fund` trước, tên chain không gạch nối, genesis ≤ 10 KB (trần cứng 256 KiB), runbook
"nạp phí trước restart", knob cache giữ như nắp. Chi phí máy có thể **giảm**: 15 sổ/node cần ~1 GB RAM, ~0,3 nhân ⇒ máy
AX52 chứa **8 node** thay vì 4 ⇒ 72 node = **9 máy** (~€540/tháng), hoặc VM 2 vCPU · 4 GB.

## Việc netgen dạy (ghi cho `main`)

- `SUBNET_PREFIX` phải kết thúc `.0` (`172.31` ⇒ IP `172.31.11` sai; không có cổng nào bắt).
- Bản tập mặc định mang chainId C-Chain **thật** `9000000009`; cổng patch 0015 **cảnh báo** rồi vẫn sinh. Phải
  đặt `A1_CHAIN_ID` tay.
- Dòng định danh in *"khối chainId L1 9001000000–9001999999"* cho **băng tập** — trùng khối g1 thật (Adam Chain
  `9001000000`). L1 tập trong kit dùng `8990000001+`. 🔴 Đề nghị netgen in khối L1 **riêng** cho băng tập.

## Còn chạy / chưa đo

0.1b RAM 10 plugin/node **dưới tải** (knob K1 rồi knob mặc định) · 0.3 resume `apply` · 0.4c `topup` từ ví khác ⇒
thức · 0.6b 5 sổ genesis 505 KB · 0.7 thời gian bootstrap 10 chain/node sau restart.
